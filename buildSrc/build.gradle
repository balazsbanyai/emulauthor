plugins {
    id 'java-gradle-plugin'
}

gradlePlugin {
    plugins {
        emulauthor {
            id = 'com.banyaibalazs.emulauthor'
            implementationClass = 'com.banyaibalazs.emulauthor.Emulauthor'
        }
    }
}